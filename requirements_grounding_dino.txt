# requirements_grounding_dino.txt
# ─────────────────────────────────────────────────────────────────
# Compute: NVIDIA RTX A5000 (24GB), CUDA 13.0, Driver 580.95.05
# Python:  3.10
# ─────────────────────────────────────────────────────────────────
#
# This is the lightest env of the set (~550MB VRAM total).
# Can share the radvlm conda env if desired (transformers==4.46.0
# is compatible with Grounding DINO), or create a dedicated one.
#
# Setup (dedicated env):
#   conda create -n gdino python=3.10 -y
#   conda activate gdino
#
# Step 1 — PyTorch with CUDA 12.4 wheels (compatible with CUDA 13.0 driver):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Step 2 — install everything else:
#   pip install -r requirements_grounding_dino.txt
#
# No gated HF access needed — both models are fully open.
# ─────────────────────────────────────────────────────────────────

# Grounding DINO (GroundingDinoForObjectDetection / post_process_grounded_
# object_detection) was merged into transformers on 2024-04-11 and first
# shipped in the 4.40.0 release -- it does NOT exist in 4.38/4.39, despite
# earlier guidance in this file suggesting 4.38+. Pin to >=4.40.0.
# (transformers 4.51.0 changed post_process_grounded_object_detection to
# return integer label ids instead of strings in some paths; harmless here
# since we only use `scores`/`boxes`, but noted for anyone parsing `labels`.)
transformers>=4.40.0

accelerate>=0.25.0
pydicom>=2.4.0
Pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
