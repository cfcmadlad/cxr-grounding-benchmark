# requirements_chexagent.txt
# ─────────────────────────────────────────────────────────────────
# Compute: NVIDIA RTX A5000 (24GB), CUDA 13.0, Driver 580.95.05
# Python:  3.10
# ─────────────────────────────────────────────────────────────────
#
# Setup:
#   conda create -n chexagent python=3.10 -y
#   conda activate chexagent
#
# Step 1 — PyTorch with CUDA 12.4 wheels (compatible with CUDA 13.0 driver):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Step 2 — install everything else:
#   pip install -r requirements_chexagent.txt
#
# No gated HF access needed — CheXagent-8b is fully open.
# ─────────────────────────────────────────────────────────────────

# CheXagent-8b was released Jan 2024. Keep transformers in a range
# that avoids conflicts with RadVLM (==4.46.0) and BioViL-T (<4.40.0).
# Tested range: 4.35 to 4.46 (separate conda env from other models).
transformers>=4.35.0,<4.47.0

accelerate>=0.25.0
pydicom>=2.4.0
Pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
